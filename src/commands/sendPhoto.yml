description: >
  Sends a photo in a given chat.
parameters:
  telegram-bot-token:
    type: env_var_name
    default: TELEGRAM_BOT_TOKEN
    description: >
      Name of environment variable storing your Telegram bot token
  telegram-chat-id:
    type: env_var_name
    default: TELEGRAM_CHAT_ID
    description: >
      Name of environment variable storing your Telegram chat id
  telegram-photo:
    description: Enter a photo file_id to send a photo that exists on the Telegram servers or an HTTP URL as a String for Telegram to get a photo from the Internet or upload using a new photo using form-data
    type: string or inputFile
    default: https://raw.githubusercontent.com/TelegramBots/book/master/src/docs/photo-ara.jpg

steps:
  - run:
      name: SendPhoto
      shell: /bin/bash
      command: |
        if [-z "$parameter.telegram-bot-token" -o -z "$parameter.telegram-chat-id" -o -z "$parameter.telegram-photo"]; then
          echo "There is a variable that is empty"
        fi
        curl "https://api.telegram.org/bot$<<parameters.telegram-bot-token>>/sendPhoto?chat_id=$<<parameters.telegram-chat-id>>&photo=<<parameters.telegram-photo>>"
